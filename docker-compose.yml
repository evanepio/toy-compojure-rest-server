version: "3"

services:
  db:
    image: postgres:9.4
    environment:
      POSTGRES_PASSWORD: pass1234
      POSTGRES_USER: toy_user
  clojure:
    build: .
    ports:
      - 3000:3000
    environment:
      POSTGRES_PASSWORD: pass1234
      POSTGRES_USER: toy_user
